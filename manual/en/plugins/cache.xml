<?xml version="1.0" encoding="UTF-8"?>
<chapter id="cache" xmlns="http://docbook.org/ns/docbook">
	<title>Cache</title>
	<para>
		This plugin provides a very simple caching mechanism. Request to the application are cached for a
		certain amount of time.
	</para>
	<para>
		Cached requests are stored on the disk in the folder specified in the <quote>dir</quote>
		condifuration key. The default is <filename>app/cache</filename>.
	</para>
	<para>
		The request is cached based on the REQUEST_URI php variable.
	</para>
	<para>
		By default, a request is cached for an hour. This can be changed using the <quote>default_time</quote>
		configuration key. The caching time can also be specified on a per request basis. The time is specified
		in seconds.
	</para>
	<para>
		To specify which action should be cached add them to the <quote>requests</quote> configuration key as a
		key to the array. The associated value should be the cached time. Specify 0 to use the default time,
		-1 for definitive caching or any other value.
	</para>
	<example>
		<title>Caching requests</title>
		<programlisting>
			<![CDATA[
Atomik::set('plugins/Cache/requests', array(
	'home' => 300, // cache the home for 5 minutes
	'my-article' => -1 // cache my-article for un unlimited amount of time
));]]>
		</programlisting>
	</example>
</chapter>